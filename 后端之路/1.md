# [FastAPI系列]基础

首先，我们需要安装FastAPI。

Windows运行：

```PowerShell
pip install "fastapi[standard]"
```

MacOS/Linux运行：

```Shell
pip3 install "fastapi[standard]"
```

装好之后，我们就可以新建一个文件`main.py`，然后不管三七四十九，在开头写上：

```Python
from fastapi import FastAPI

app = FastAPI()
```

后面的下一期再说。
